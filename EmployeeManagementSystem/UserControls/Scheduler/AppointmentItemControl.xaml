<UserControl x:Class="EmployeeManagementSystem.UserControls.Scheduler.AppointmentItemControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EmployeeManagementSystem.UserControls.Scheduler"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="200">
    <Border Background="{Binding Hex}"
            CornerRadius="5"
            Margin="-5 0 5 0"
            BorderBrush="{StaticResource DarkBlueBrush}"
            BorderThickness="1">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
        
            <TextBlock Grid.Column="0"
                       Text="{Binding Name, FallbackValue=John_Doe}"
                       Margin="3"
                       FontSize="12"
                       VerticalAlignment="Center"/>

        </Grid>
    </Border>
</UserControl>

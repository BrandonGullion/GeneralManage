<UserControl x:Class="EmployeeManagementSystem.UserControls.UserSettingsControls.UserSettingsItemListControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EmployeeManagementSystem.UserControls.UserSettingsControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Border Background="Transparent"
            CornerRadius="5">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="{Binding UserFirstName}"
                       Margin="5"
                       Grid.Column="0"
                       FontSize="14"/>
            <TextBlock Text="{Binding UserLastName}"
                       Margin="5"
                       Grid.Column="1"
                       FontSize="14"/>
            <TextBlock Text="{Binding AuthorityLevel}"
                       Margin="5"
                       Grid.Column="2"
                       FontSize="14"/>
        </Grid>
    </Border>

</UserControl>
